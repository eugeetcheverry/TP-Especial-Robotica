function particles = initialize_particles(count)
    % Returns a set of randomly initialized particles.
    particles = [
        normrnd(15.0, 2, count, 1), ...
        normrnd(15.0, 2, count, 1), ...
        unifrnd(-pi, pi, count, 1)
    ];
end

